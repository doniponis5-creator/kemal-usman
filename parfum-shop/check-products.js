#!/usr/bin/env node
// DEPRECATED — hardcoded HTTP backend URL.
//   curl -s "$PB_URL/api/collections/products/records?filter=(name='Sauvage')" | jq
console.error('❌ check-products.js is deprecated.');
process.exit(1);
